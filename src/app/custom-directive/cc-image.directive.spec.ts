import { CcImageDirective } from './cc-image.directive';

describe('CcImageDirective', () => {
  it('should create an instance', () => {
    const directive = new CcImageDirective();
    expect(directive).toBeTruthy();
  });
});

import { CcdDirective } from './ccd.directive';

describe('CcdDirective', () => {
  it('should create an instance', () => {
    const directive = new CcdDirective();
    expect(directive).toBeTruthy();
  });
});
